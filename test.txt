#標準入力1行目の3つの値を取得する。
#二次元リスト（cards）を作り、値を取得する。
#プレイヤーをキー、獲得枚数を値とした辞書(points)を作る。
#手番の数(num)を取得する。
各手番について以下の手順を行う。「
#手番となるプレイヤーを％とcountで指定する。
#cardsからindexを指定して2枚(a,A)引く。
#2枚が同じ数字か判定する。
#同じならpointsに2を加える。」
pointsを出力する。
